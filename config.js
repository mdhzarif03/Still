// Paste your Spotify app's Client ID between the quotes, then redeploy.
// It's a public identifier (not a secret), so it's safe to commit.
// Leave it empty and Still will ask for it inside the app instead.
window.STILL_CONFIG = {
  spotifyClientId: ''
};
